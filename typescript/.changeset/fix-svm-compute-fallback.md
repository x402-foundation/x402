---
"@x402/svm": patch
---

Fixed exact-SVM verification reporting `smart_wallet_program_not_allowed` against the SPL Token program for a direct token transaction; the Path 1 layout reason is now returned instead.
