# frozen_string_literal: true

source 'https://rubygems.org'

gemspec

# Optional blockchain dependencies (users install as needed)
group :evm, optional: true do
  gem 'eth', '~> 0.5'
end

group :svm, optional: true do
  gem 'base58', '~> 0.2'
  gem 'ed25519', '~> 1.3'
end

# Optional web frameworks (for middleware testing)
group :development do
  gem 'rack', '~> 3.0'
  gem 'sinatra', '~> 3.0'
end
