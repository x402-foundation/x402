Fail closed when a batch-settlement refund receipt has no positive Refunded amount. Expose FacilitatorWeb3Signer.web3 and compare indexed bytes32 channel ids as hex so the SDK signer actually decodes.
