EVM_ADDRESS=0x...
FACILITATOR_URL=http://localhost:4022

# Required for delegated-sync and delegated-deferred (signs capture/void/refund).
EVM_RECEIVER_AUTHORIZER_PRIVATE_KEY=0x...

# Custom-escrow flow only: permissionless collect wrapper on the facilitator allowlist.
CUSTOM_OPERATOR_ADDRESS=
